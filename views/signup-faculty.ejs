
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>CAS - Faculty Signup</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="relative flex items-center justify-center min-h-screen bg-cover bg-center"
      style="background-image: url('/images/opdp.jpg');">
      

      <div class="relative z-10 bg-white/90 backdrop-blur-md w-[520px] rounded-3xl shadow-2xl p-10">

    
    <div class="flex justify-center mb-6">
      <div class="w-24 h-24 bg-green-800 rounded-3xl flex items-center justify-center text-white text-4xl">
        🔖
      </div>
    </div>

 
    <h1 class="text-3xl font-bold text-center mb-2">CAS</h1>
    <p class="text-center text-gray-500 mb-8">
      Login as Faculty / Instructor
    </p>

 
    <% if(error){ %>
      <div class="bg-red-100 text-red-700 p-3 rounded-xl mb-4 text-sm text-center">
        <%= error %>
      </div>
    <% } %>


    <form action="/faculty-signup" method="POST" class="space-y-5">

      <input type="text" name="fullName" placeholder="Full Name" required
        class="w-full border border-gray-300 rounded-2xl px-6 py-4 focus:outline-none focus:ring-2 focus:ring-green-700">

        <input type="email"
        name="email"
        placeholder="Email (e.g., name@sharjah.ac.ae)"
        required
        pattern="^[a-zA-Z0-9._%+-]+@sharjah\.ac\.ae$"
        title="Email must be in format: name@sharjah.ac.ae"
        class="w-full border border-gray-300 rounded-2xl px-6 py-4 focus:outline-none focus:ring-2 focus:ring-green-700">
 

      <input type="text" name="username" placeholder="Username" required
        class="w-full border border-gray-300 rounded-2xl px-6 py-4 focus:outline-none focus:ring-2 focus:ring-green-700">

      <input type="password" name="password" placeholder="Password" required
        class="w-full border border-gray-300 rounded-2xl px-6 py-4 focus:outline-none focus:ring-2 focus:ring-green-700">

      <input type="password" name="confirmPassword" placeholder="Confirm Password" required
        class="w-full border border-gray-300 rounded-2xl px-6 py-4 focus:outline-none focus:ring-2 focus:ring-green-700">

      <div class="flex gap-4 mt-6">

        <a href="/faculty-login"
           class="w-1/2 bg-gray-200 text-center py-4 rounded-2xl font-medium hover:bg-gray-300 transition">
          Back
        </a>

        <button type="submit"
          class="w-1/2 bg-green-800 text-white py-4 rounded-2xl font-medium hover:bg-green-900 transition">
          Sign Up
        </button>

      </div>

    </form>

  </div>

  <script>
    const emailInput = document.querySelector("input[name='email']");
  
    emailInput.addEventListener("input", function () {
      if (!emailInput.value.endsWith("@sharjah.ac.ae")) {
        emailInput.setCustomValidity("Email must be a valid sharjah.ac.ae email");
      } else {
        emailInput.setCustomValidity("");
      }
    });
  </script>
  

</body>
</html>
