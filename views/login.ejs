<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Faculty Login</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-gray-200 flex items-center justify-center min-h-screen">

<div class="bg-white shadow-xl rounded-2xl w-[420px] p-8">

    <div class="flex flex-col items-center mb-6">
        <div class="bg-green-700 w-16 h-16 rounded-xl flex items-center justify-center text-white text-2xl">
            🎓
        </div>
        <h2 class="text-2xl font-bold mt-4">CAS</h2>
        <p class="text-gray-500 text-sm">Login as Faculty / Instructor</p>
    </div>

    <% if(error) { %>
        <p class="text-red-500 text-sm mb-4 text-center"><%= error %></p>
    <% } %>

    <form action="/login" method="POST">

        <label class="block font-medium mb-1">Username</label>
        <input type="text" name="username" required
            class="w-full border-2 rounded-lg px-4 py-3 mb-4 focus:outline-none focus:ring-2 focus:ring-green-600">

        <label class="block font-medium mb-1">Password</label>
        <input type="password" name="password" required
            class="w-full border-2 rounded-lg px-4 py-3 mb-6 focus:outline-none focus:ring-2 focus:ring-green-600">

        <div class="flex justify-between gap-4">
            <a href="/" class="border-2 px-6 py-3 rounded-lg w-full text-center">
                Back
            </a>

            <button type="submit"
                class="bg-blue-900 text-white px-6 py-3 rounded-lg w-full">
                Sign In
            </button>
        </div>
        <p class="text-center text-sm text-gray-500 mt-6">
            Don't have an account?
            <a href="/signup" class="font-semibold text-blue-900">Sign Up</a>
        </p>
        

    </form>

</div>

</body>
</html>
