<!DOCTYPE html>
<html>
<head>
  <title>CAS - HOD Panel</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">

  <!-- HOD NAVBAR -->
  <nav class="bg-white shadow px-10 py-4 flex justify-between items-center">

    <div class="flex items-center gap-6">

        <h1 class="text-xl font-bold text-blue-950">CAS - HOD Panel</h1>
      
        <!-- Dashboard -->
        <a href="/hod-dashboard"
           class="<%= currentPage === 'dashboard' ? 
           'bg-blue-900 text-white px-4 py-2 rounded-lg' : 
           'hover:text-blue-900' %>">
          Dashboard
        </a>
      
        <!-- Allocations -->
        <a href="/hod-allocations"
           class="<%= currentPage === 'allocations' ? 
           'bg-blue-900 text-white px-4 py-2 rounded-lg' : 
           'hover:text-blue-900' %>">
          Allocations
        </a>
      
        <!-- Faculty -->
        <a href="/faculty-members"
           class="<%= currentPage === 'faculty' ?
           'bg-blue-900 text-white px-4 py-2 rounded-lg' :
           'hover:text-blue-900' %>">
          Faculty
        </a>
      
      </div>
      

    <div class="flex items-center gap-6">

      <div class="text-right">
        <p class="font-semibold"><%= data.user.name %></p>
        <p class="text-sm text-gray-500">HOD</p>
      </div>

      <a href="/logout" class="text-red-600 hover:text-red-800">
        Logout →
      </a>

    </div>

  </nav>

  <!-- PAGE CONTENT -->
  <div>
    <%- body %>
  </div>

</body>
</html>
